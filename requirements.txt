# Clinical Data Pipeline - Python Dependencies
# Install with: pip install -r requirements.txt

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Synthetic data generation
Faker>=18.9.0

# Database connectivity (SQLite is built-in to Python)
# No additional package needed for SQLite

# Optional: If you later want to export to SAS format
# pyreadstat>=1.2.0

# Optional: For data validation
# pydantic>=2.0.0

# Optional: For YAML config files (if you add DMP config)
# pyyaml>=6.0